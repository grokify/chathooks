# Runscope Webhook Notes

* `response_status_code` is a string in webhook, not number
* `,` is needed after `variables`
* `initial_variables` and `variables` have unknown format. Are these `map[string]string`?